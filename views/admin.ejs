<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administração</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="bg-white w-4/5 mx-auto shadow-lg my-6 rounded-lg overflow-hidden py-14 px-32">
        <h1 class="text-2xl font-bold mb-4"><a href="/" class="hover:underline">Games Hub</a> - Administração de Jogos</h1>
        <a href="/admin/jogos/adicionar" class="bg-blue-500 text-white rounded p-2 mb-4 inline-block">Adicionar Jogo</a>
    
        <h2 class="text-xl font-bold mb-4">Jogos Cadastrados</h2>
    
        <!-- Filtro de ordenação -->
        <form action="/admin/jogos" method="GET" class="mb-4">
            <label for="sort" class="mr-2">Ordenar por:</label>
            <select name="sort" id="sort" class="border rounded p-2">
                <option value="data_criacao" <% if (sort === 'data_criacao') { %> selected <% } %>>Data de Criação</option>
                <option value="data_modificacao" <% if (sort === 'data_modificacao') { %> selected <% } %>>Data de Modificação</option>
            </select>
            <button type="submit" class="bg-blue-500 text-white rounded p-2 ml-2">Ordenar</button>
        </form>
    
        <table class="w-full bg-white shadow-md rounded mb-4">
            <thead>
                <tr>
                    <th class="border p-2 text-center">Nome</th>
                    <th class="border p-2 text-center">Data de Criação</th>
                    <th class="border p-2 text-center">Data de Modificação</th>
                    <th class="border p-2 text-center">Ações</th>
                </tr>
            </thead>
            <tbody>
                <% jogos.forEach(jogo => { %>
                    <tr>
                        <td class="border p-2 text-center"><%= jogo.nome %></td>
                        <td class="border p-2 text-center"><%= new Date(jogo.data_criacao).toLocaleString() %></td>
                        <td class="border p-2 text-center"><%= new Date(jogo.data_modificacao).toLocaleString() %></td>
                        <td class="border p-2 text-center">
                            <a href="/admin/jogos/editar/<%= jogo.slug%>" class="bg-yellow-500 text-white rounded p-2 inline-block">Editar</a>
                            <form action="/admin/jogos/<%= jogo.id %>/delete" method="POST" class="inline">
                                <button type="submit" class="bg-red-500 text-white rounded p-2 inline-block">Excluir</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        
    </div>
    
</body>
</html>
