<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= conteudo.titulo %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-4">
    <div class="max-w-2xl mx-auto bg-white shadow-lg rounded-lg overflow-hidden">
        <img class="w-full h-64 object-cover" src="public/uploads/<%= conteudo.imagem %>" alt="<%= conteudo.titulo %>">
        <div class="p-4">
            <h1 class="text-3xl font-bold mb-4"><%= conteudo.titulo %></h1>
            <p class="text-gray-700 mb-4"><%= conteudo.descricao %></p>
            <p class="text-gray-700 mb-4"><strong>Ano de Lançamento:</strong> <%= conteudo.ano %></p>
            <p class="text-gray-700 mb-4"><strong>Plataforma:</strong> <%= conteudo.plataforma %></p>
            <p class="text-gray-700 mb-4"><strong>Categoria:</strong> <%= conteudo.categoria %></p>
            <p class="text-gray-700 mb-4"><strong>Desenvolvedor:</strong> <%= conteudo.desenvolvedor %></p>
            <% if (conteudo.links && conteudo.links.length > 0) { %>
                <div class="mb-4">
                    <h2 class="text-2xl font-bold mb-2">Links Externos</h2>
                    <ul class="list-disc list-inside">
                        <% conteudo.links.forEach(link => { %>
                            <li><a href="<%= link.url %>" class="text-blue-500" target="_blank"><%= link.title %></a></li>
                        <% }) %>
                    </ul>
                </div>
            <% } %>
            <% if (conteudo.imagens && conteudo.imagens.length > 0) { %>
                <div class="mb-4">
                    <h2 class="text-2xl font-bold mb-2">Imagens</h2>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <% conteudo.imagens.forEach(imagem => { %>
                            <img src="public/uploads/<%= imagem %>" alt="<%= conteudo.titulo %>" class="w-full h-64 object-cover">
                        <% }) %>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>