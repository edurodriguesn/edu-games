<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= jogo.titulo %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <header class="p-6 text-center text-white bg-gradient-to-r from-blue-800 to-blue-500 shadow-lg">
        <h1 class="text-3xl font-semibold">
            <a href="/" class="hover:underline">Games Hub</a> - Central de Jogos sobre Computação
        </h1>
    </header>
    
    <main class="w-[65%] py-6 px-20 my-8 mx-auto bg-white shadow-lg rounded-lg overflow-hidden">
    
        <!-- Imagem de capa -->
        <% if (jogo.imagens && jogo.imagens.length > 0) { %>
            <div class="mb-8">
                <img src="data:image/jpeg;base64,<%= jogo.imagens[0] %>" alt="Capa do Jogo" class="w-full h-96 object-cover rounded-lg shadow-md">
            </div>
        <% } %>
    
        <div class="p-6">
            <h1 class="text-5xl font-bold text-center text-gray-800 mb-4"><%= jogo.nome %></h1>
        </div>
        
        <div class="w-2/3 p-6 mx-auto text-lg text-gray-700 bg-blue-50 rounded-lg shadow-md">
            <p class="mb-2"><strong>Ano de Lançamento:</strong> <%= jogo.ano %></p>
            <p class="mb-2"><strong>Plataforma:</strong> <%= Array.isArray(jogo.plataformas) ? jogo.plataformas.join(', ') : 'N/A' %></p>
            <p class="mb-2"><strong>Categoria:</strong> <%= Array.isArray(jogo.categorias) ? jogo.categorias.join(', ') : 'N/A' %></p>
            <p class="mb-4"><strong>Desenvolvedor:</strong> <%= Array.isArray(jogo.desenvolvedores) ? jogo.desenvolvedores.join(', ') : 'N/A' %></p>
        </div>
    
        <div class="p-6">
            <p class="text-xl text-gray-700 mb-4"><%- jogo.descricao.replace(/\n/g, '<br>') %></p>
    
            <!-- Links Externos -->
            <% if (jogo.links && jogo.links.length > 0) { %>
                <div class="mb-4">
                    <h2 class="text-3xl font-bold mb-4">Links Externos</h2>
                    <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
                        <% jogo.links.forEach(link => { %>
                            <div class="p-4 bg-white shadow-md rounded-lg transform transition-transform hover:scale-105">
                                <h3 class="text-xl font-semibold mb-2"><%= link.title %></h3>
                                <a href="<%= link.url %>" class="text-blue-500 hover:underline" target="_blank">Visitar</a>
                            </div>
                        <% }) %>
                    </div>
                </div>
            <% } %>
        </div>
    
        <!-- Screenshots -->
        <% if (jogo.imagens && jogo.imagens.length > 1) { %>
            <div class="p-6 bg-gray-50">
                <h2 class="text-3xl font-bold mb-2">Screenshots</h2>
                <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
                    <% jogo.imagens.slice(1).forEach((imagemBase64, index) => { %>
                        <img src="data:image/jpeg;base64,<%= imagemBase64 %>" alt="imagem-<%= index + 1 %>" class="w-full h-64 object-cover rounded-lg shadow-md">
                    <% }) %>
                </div>
            </div>
        <% } %>
    </main>
    
    <footer class="p-4 text-white bg-gradient-to-r from-blue-800 to-blue-500">
        <div class="w-[65%] mx-auto flex items-center">
            <div class="flex justify-center items-center">
                <a href="/" class="hover:underline">
                    <img src="../images/logo.png" alt="logo" class="w-32">
                </a>
            </div>
    
            <div class="flex flex-col w-full ml-4">
                <div class="flex justify-between mb-10">
                    <p>&copy; 2024 <a href="/" class="hover:underline">Games Hub</a>. Todos os direitos reservados.</p>
                    <div class="flex space-x-4">
                        <a href="/sobre" class="hover:underline">Sobre Nós</a>
                        <a href="/contato" class="hover:underline">Contato</a>
                        <a href="/politica-privacidade" class="hover:underline">Política de Privacidade</a>
                        <a href="https://www2.unifap.br/ccomputacao/" class="hover:underline">Site do Curso</a>
                    </div>
                </div>
    
                <div class="mt-4 text-center text-gray-200">
                    <p>Ciência da Computação | Universidade Federal do Amapá</p>
                    <p>Endereço: Rodovia Josmar Chaves Pinto - KM 02, 68903-419 - Macapá - AP</p>
                </div>
            </div>
        </div>
    </footer>    
</body>
</html>
